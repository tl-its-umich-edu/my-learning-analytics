{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "api docker",
            "type": "python",
            "request": "attach",
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "/dashboard"
                }
            ],
            "port": 3001,
            "host": "localhost",
        }
    ]
}
